import Link from "next/link";
import { realmCharacters } from "@/data/realm";
import { CharacterLoreCard } from "@/components/lore-cards";
export default async function CharactersPage({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; const he = locale === "he"; return <main className="realm-page" dir={he ? "rtl" : "ltr"}><header className="book-header"><Link href={`/${locale}`}>כס התבונה <span>THE INTELLIGENCE THRONE</span></Link><Link href={`/${locale}/realm`}>{he ? "שבעת הכוחות" : "Seven powers"}</Link></header><section className="realm-intro"><span>PEOPLE / SYSTEMS / CONSEQUENCES</span><h1>{he ? "האנשים מאחורי הכוח." : "The people behind power."}</h1><p>{he ? "כל דמות נפתחת בשתי שכבות: מי היא בעולם, ומה היא שואלת אותך." : "Every character opens in two layers: who they are in the world, and what they ask of you."}</p></section><div className="portrait-grid lore-grid">{realmCharacters.map((character) => <CharacterLoreCard character={character} he={he} key={character[0]} />)}</div></main>; }
