import Link from "next/link";
import { bookUnits, parts } from "@/data/book";
export default async function BookPage({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; const he = locale === "he"; return <main className="book-page" dir={he ? "rtl" : "ltr"}><header className="book-header"><Link href={`/${locale}`}>כס התבונה <span>THE INTELLIGENCE THRONE</span></Link><Link href={`/${locale}/read/beyond-the-threshold`}>{he ? "התחל לקרוא" : "Start reading"}</Link></header><section className="book-intro"><span>THE BOOK</span><h1>{he ? "הספר נפתח פרק אחר פרק." : "The book opens one chapter at a time."}</h1><p>{he ? "כל 27 יחידות הקריאה נמצאות במערכת. רק יחידות שאושרו נפתחות." : "All 27 reading units live in the system. Only approved units are open."}</p></section>{parts.map((part, index) => <section className="part-shelf" key={part}><div className="part-label"><span>PART {index + 1}</span><h2>{part}</h2></div><div className="unit-list">{bookUnits.filter((unit) => unit.part === index + 1).map((unit) => <article className={`unit ${unit.status}`} key={unit.slug}><div className="unit-number">{unit.kind === "prologue" ? "00" : unit.kind === "epilogue" ? "EP" : String(unit.number).padStart(2, "0")}</div><div><span>{unit.status === "published" ? (he ? "פתוח לקריאה" : "Available") : unit.status === "scheduled" ? (he ? "בקרוב" : "Scheduled") : (he ? "נעול" : "Locked")}</span><h3>{he ? unit.titleHe : unit.titleEn}</h3><p>{he ? unit.titleEn : unit.titleHe}</p></div>{unit.status === "published" ? <Link href={`/${locale}/read/${unit.slug}`}>{he ? "קרא" : "Read"}</Link> : <i>◇</i>}</article>)}</div></section>)}</main>; }
